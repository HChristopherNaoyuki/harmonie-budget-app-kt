<?xml version="1.0" encoding="utf-8"?>
<!--
    Drawable for the category Spinner background.
    Provides a light gray background with a dropdown arrow indicator on the right side.
    This ensures users can visually identify the Spinner as an interactive dropdown element.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape with rounded corners -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#F0F0F0" />
            <corners android:radius="4dp" />
            <stroke android:width="1dp" android:color="#6A1B9A" />
        </shape>
    </item>
    <!-- Dropdown arrow icon positioned on the right side -->
    <item
        android:drawable="@android:drawable/arrow_down_float"
        android:gravity="center_vertical|end"
        android:right="12dp" />
</layer-list>