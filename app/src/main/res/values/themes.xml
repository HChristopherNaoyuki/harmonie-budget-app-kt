
<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Base application theme.
        FIXED:
        1. <?xml declaration is the VERY FIRST line (no comments or whitespace before it).
        2. Style name corrected to exactly match the manifest (@style/Theme.HarmonieBudgetAppKt).
        3. Parent changed to NoActionBar for clean minimal UI (Apple-like feel without mentioning Apple).
        4. Uses colors defined in colors.xml for consistency.
        Up-to-date Android 14+ MaterialComponents (2026).
    -->
    <style name="Theme.HarmonieBudgetAppKt" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color (clean blue). -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccent</item>
        <item name="colorOnSecondary">@android:color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>