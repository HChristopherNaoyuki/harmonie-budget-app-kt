<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Night theme for the app.
        This file defines the dark theme used when the device is in night mode.
        It inherits from the standard MaterialComponents night theme.
        No unused namespace declarations are present.
        This resolves the "Namespace declaration is never used" warning.
        The theme is applied automatically by the system when night mode is active.
    -->
    <style name="Theme.HarmonieBudgetAppKt" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Night theme colors are inherited from the parent theme. -->
        <!-- Additional custom night-mode overrides can be added here if needed in future updates. -->
    </style>

</resources>