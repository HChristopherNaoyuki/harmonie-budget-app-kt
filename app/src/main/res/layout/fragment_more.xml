<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#F5F5F5">

    <!-- Each item is a collapsible panel following progressive disclosure from the User Interface.pdf (page 3) -->

    <androidx.cardview.widget.CardView
        android:id="@+id/card_export"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        app:cardCornerRadius="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_export_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="Export Data"
                android:textSize="18sp" />

            <LinearLayout
                android:id="@+id/layout_export_content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                android:padding="16dp">

                <!-- Content revealed on tap (user-specific export info) -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Your data has been exported to JSON files in the budget_data folder." />
            </LinearLayout>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Similar CardView for Receive Progress, About, Help Information, Version -->

    <androidx.cardview.widget.CardView
        android:id="@+id/card_receive"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        app:cardCornerRadius="8dp">
        <!-- Title and hidden content for Receive Progress -->
    </androidx.cardview.widget.CardView>

    <!-- Repeat for About, Help Information, Version with their respective titles and content areas -->

</LinearLayout>