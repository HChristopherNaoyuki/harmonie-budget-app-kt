<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!--
        Category management layout.

        FIXED:
        1. Added proper xmlns:app namespace declaration on the root ConstraintLayout.
        2. This resolves the XMLStreamException: "AttributePrefixUnbound" for app:layout_constraint* attributes.
        3. All views use explicit android:layout_width/height and full constraints (no design-time only positions).
        4. Wrapped EditText in TextInputLayout for Material design, accessibility, and hint support.
        5. Used @string references to eliminate hardcoded strings.
        6. Layout follows Android 14+ best practices (ConstraintLayout 2.1+, MaterialComponents).
        7. Edge case: works on all screen sizes and orientations.
    -->

    <!-- Category name input -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/categoryNameLayout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etCategoryName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/hint_category_name"
            android:inputType="text" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Add category button -->
    <Button
        android:id="@+id/btnAddCategory"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/btn_add_category"
        app:layout_constraintTop_toBottomOf="@id/categoryNameLayout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        style="@style/Widget.MaterialComponents.Button" />

</androidx.constraintlayout.widget.ConstraintLayout>